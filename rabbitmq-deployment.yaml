apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
  namespace: shoppingcart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rabbitmq
  template:
    metadata:
      labels:
        app: rabbitmq
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:3-management
          ports:
            - containerPort: 5672   # application port
            - containerPort: 15672  # management UI port